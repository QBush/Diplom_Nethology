<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/common_spacing"
    tools:context=".Main.AllRecipesFragment"
    android:id="@+id/favoritesRecipesFragment">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/favoriteRecipesHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/favorites"
        app:layout_constraintTop_toTopOf="parent"
        android:gravity="center"
        />

    <ImageView
        android:id="@+id/favoriteRecipesFullPicture"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@+id/favoriteRecipesHeader"
        app:srcCompat="@drawable/favorites_full_picture" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/PostsRecycleView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:layout_editor_absoluteX="25dp"
        tools:layout_editor_absoluteY="-56dp"
        tools:listitem="@layout/recipes_list_item" />

</androidx.constraintlayout.widget.ConstraintLayout>
